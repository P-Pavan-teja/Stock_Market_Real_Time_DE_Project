name: stocks_dbt
version: 1.0.0
config-version: 2
profile: stocks_dbt
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots
models:
  your_project_name:
    staging:
      +schema: COMMON
